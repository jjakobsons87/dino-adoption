import React from 'react';
import { Navigate, useParams } from 'react-router-dom';


//components

const Cart = (props) => {
    return (
        <div className="cart-container">
        </div>
    )
};

export default Cart;