import React from 'react';


const cartItems = () => {

    return (
        <div className="cart-item-card">
            {/* placeholder to import selected dino from queries */}
            {/* placeholder to import selected dino name from queries */}
            <div className="quantity">Quantity:
            </div>
            <div className="total">Cost:
            </div>
        </div>
    )
};

export default cartItems;